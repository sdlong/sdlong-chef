{
  "id": "deploy",
  // generate this with: openssl passwd -1 "plaintextpassword"
  "password": "your_password",
  // the below should contain a list of ssh public keys which should
  // be able to login as deploy
  "ssh_keys": ["your_ssh_pub_key"
  ],
  "groups": [ "sysadmin"],
  "shell": "\/bin\/bash"
}